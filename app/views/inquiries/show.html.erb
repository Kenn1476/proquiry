<div class="contents row">
    <div class="more">
      <ul class="more_list">
        <li>
          <%= link_to '編集', edit_inquiry_path(@inquiry.id) %>
        </li>
        <li>
          <%= link_to '削除', inquiry_path(@inquiry.id), data: { turbo_method: :delete } %>
        </li>
      </ul>
    </div>
    <p><%= @inquiry.text %></p>
    <span class="name"><%= @inquiry.name %>
    </span>
</div>